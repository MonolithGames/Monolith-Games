name: Monolith Engine Build

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Install build tools
      run: sudo apt-get update && sudo apt-get install -y build-essential

    - name: Build Monolith Engine
      run: make

    - name: Move binary to build folder
      run: |
        mkdir -p build
        cp monolith_engine build/

    - name: Commit build artifact back to repo
      run: |
        git config user.name "github-actions"
        git config user.email "actions@github.com"
        git add build/monolith_engine
        git commit -m "Automated build update"
        git push
