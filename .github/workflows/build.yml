name: Alphabet Media Build (Windows)

permissions:
  contents: write

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Install MinGW (Windows GCC)
      run: choco install mingw -y

    - name: Build Alphabet Media (Windows .exe)
      run: make OUT=Alphabet_Media.exe

    - name: Move binary to build folder
      run: |
        if (!(Test-Path build)) { New-Item -ItemType Directory -Path build }
        Copy-Item Alphabet_Media.exe build\

    - name: Commit build artifact back to repo
      run: |
        git config user.name "github-actions"
        git config user.email "actions@github.com"
        git add build/Alphabet_Media.exe
        git commit -m "Automated Windows build for Alphabet Media"
        git push
