name: Hello World Build (Windows)

permissions:
  contents: write

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Install MinGW (Windows GCC)
      run: choco install mingw -y

    - name: Build Hello World (Windows .exe)
      run: |
        gcc src/main.c -o hello_world.exe

    - name: Move binary to build folder
      run: |
        if (!(Test-Path build)) { New-Item -ItemType Directory -Path build }
        Copy-Item hello_world.exe build\

    - name: Commit build artifact back to repo
      run: |
        git config user.name "github-actions"
        git config user.email "actions@github.com"
        git add build/hello_world.exe
        git commit -m "Automated Windows build"
        git push
